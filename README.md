# Sistema IDAM

Projeto organizado em pastas separadas:

- `frontend/`: aplicação React + Vite.
- `backend/`: reservado para a API futuramente.

## Comandos

```bash
npm run dev
npm run build
npm run lint
```

Os scripts da raiz encaminham a execução para o projeto em `frontend/`.
